const os = require('os')

const user = os.userInfo()
console.log(user)


console.log(`The System uptime is ${os.uptime()} seconds`)

const currOs = {
    name:os.type(),
    releae:os.release(),
    totalmem:os.totalmem(),
    freeMem:os.freemem(),
}

console.log(currOs)